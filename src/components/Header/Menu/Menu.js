import React from "react";
import { MenuContainer } from "../HeaderElements";

const Menu = () => {
  return (
    <MenuContainer>
      <div></div>
    </MenuContainer>
  );
};

export default Menu;
